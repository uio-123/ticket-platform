# 简历项目素材 · 热门赛事与演出门票交易平台

> 用途：提炼技术亮点、打磨可写进简历的项目表述。
> 输出位置：`简历项目素材-黑马商城微服务.md`

---

## 一、项目一句话定位（可直接引用）

**热门赛事与演出门票交易平台**：以 Spring Cloud Alibaba 技术栈为核心的高并发分布式票务系统，覆盖 **用户、活动票种、购票清单、订单、支付** 五大核心业务域，采用 Nacos 注册中心与配置中心、Spring Cloud Gateway 网关、OpenFeign 服务调用、Seata 分布式事务、RabbitMQ 延迟消息与异步解耦，配合 Elasticsearch 活动搜索、Redis 热点缓存、Sentinel 限流降级，实现了“检索活动—锁定票源—创建订单—支付—超时释放票源”的完整交易闭环。

**简历用一句话版：**
> 负责热门赛事与演出门票交易平台的微服务化开发，基于 Spring Cloud Alibaba 搭建订单、支付、活动票务、购票清单、用户五大微服务，通过网关统一鉴权、Seata 保证跨服务事务一致性、RabbitMQ 实现订单超时释放票源，配合 ES 活动搜索与 Redis 热点缓存，支撑高并发抢票与支付闭环。

**业务口径说明：** 简历与面试中将“商品”表述为“活动/票种/票档”，将“购物车”表述为“购票清单”，将“库存”表述为“可售票源/可售名额”。代码中的 `item`、`cart` 等命名保留为内部实现名称，不影响业务表达。

---

## 二、技术栈清单

| 类别 | 技术 | 说明 |
|---|---|---|
| 语言/构建 | Java 11 · Maven 多模块 · Spring Boot 2.7.12 | 9 个 Maven module，统一版本管理 |
| 微服务框架 | Spring Cloud 2021.0.3 · Spring Cloud Alibaba 2021.0.4.0 | |
| 注册/配置中心 | Nacos（Discovery + Config） | 服务发现、配置管理；shared-* 共享配置（jdbc/log/swagger/seata） |
| 网关 | Spring Cloud Gateway（Netty 响应式） | 全局过滤器鉴权、JWT 校验、白名单放行、user-info 透传 |
| 服务调用 | OpenFeign + spring-cloud-starter-loadbalancer | Feign 客户端 + 默认配置（FULL 日志、用户上下文拦截器） |
| 分布式事务 | Seata（AT 模式，@GlobalTransactional） | 跨服务下单事务 |
| 消息队列 | RabbitMQ | 延迟消息插件实现订单超时检测；支付成功事件解耦订单状态更新；Jackson 序列化 |
| 搜索 | Elasticsearch 7.12.1（RestHighLevelClient） | 商品索引、多条件检索、分页 |
| 缓存 | Spring Data Redis | 商品热点数据缓存 |
| 限流降级 | Sentinel | Feign 降级兜底、接口限流 |
| 持久层 | MyBatis-Plus 3.4.3 · MySQL 8.0 | 分页插件、枚举类型处理器、条件构造器 |
| 鉴权/加密 | JWT（RS256 非对称，JKS 密钥库）· BCrypt | 网关统一签发/校验 |
| 接口文档 | Knife4j / Swagger（OpenAPI 2） | 每服务独立接口文档 |
| 工具库 | Lombok · Hutool 5.8 | |

**服务清单与端口：**

| 服务 | 端口 | 职责 |
|---|---|---|
| hm-gateway | 8080 | API 网关：路由、JWT 统一鉴权、登录白名单、Sentinel 限流 |
| user-service | 8084 | 用户登录/注册、余额扣减 |
| item-service | 8081 | 活动/票种管理、ES 活动搜索、Redis 缓存、批量扣减票源（防超卖） |
| cart-service | 8082 | 购票清单增删查改、已选票种数量限制 |
| trade-service | 8085 | 购票订单创建（Seata）、订单超时检测/关单/票源回补、支付状态监听 |
| pay-service | 8086 | 支付单创建（幂等）、余额支付、状态乐观锁更新、支付成功事件通知 |
| hm-api | — | 统一 Feign 客户端与 DTO，跨服务接口定义 |
| hm-common | — | 公共组件：统一异常、统一返回、ThreadLocal 上下文、配置类 |
| hm-service | — | 单体版参考工程（教学对照，非微服务交付物） |

---

## 三、系统架构（简历配图 / 口头描述用）

```
                    ┌─────────────────────────────────────────────┐
                    │            Nacos (注册中心 + 配置中心)          │
                    │   shared-jdbc / shared-log / shared-swagger  │
                    │   shared-seata / gateway路由配置               │
                    └──────────────▲───────────────────────────────┘
                                   │ 注册/拉取配置
   客户端 ──HTTP──▶ Gateway :8080   │
        │  JWT鉴权(RS256) + 白名单   │  Redis（热门活动/票档缓存）
        │  Sentinel 限流            ▼
        │  透传 user-info 请求头   ┌──────────────┐
        ▼                          │ item-service │──ES 活动索引──▶ Elasticsearch
 ┌────────────┐       Feign + loadbalancer  :8081 │
 │ user-service│◀────┬──────────────────────────▶└──────────────┘
 │  :8084      │     │   OpenFeign 调用
 └────────────┘     │
 ┌────────────┐     │      ┌─────────────────────────────┐
 │ cart-service│◀───┴──────│ trade-service  :8085         │
 │  :8082      │  Seata 分布式事务(下单)  │  @GlobalTransactional │
 └────────────┘                 │  RabbitMQ延迟消息(订单超时)   │
                                 ▼                          │
                              pay-service :8086 ◀──────────┘
                              支付单幂等 + 乐观锁更新
                              └── MQ pay.success 事件 ──▶ trade-service 更新订单状态
```

**架构关键点（面试口径）：**
1. 网关层做 JWT 统一鉴权，避免各服务重复校验；登录白名单通过 AntPathMatcher 匹配，放行 `/items/**`、`/users/login`、`/search/**` 等公开接口。
2. 鉴权后的用户信息写入 `user-info` 请求头透传下游，服务内通过拦截器存入 ThreadLocal，Feign 调用时再由 RequestInterceptor 自动回填，实现**用户上下文跨服务透传**。
3. 抢票下单是跨 4 个服务的写操作（订单/明细落库、清购票清单、锁定票源），用 **Seata 全局事务** 保证一致性，票源扣减 SQL 加 `stock >= num` 条件天然防超卖。
4. 订单超时未支付采用 **RabbitMQ 延迟消息** 实现定时检测，超时自动关单并回补票源；支付成功后 pay-service 通过 **MQ 事件** 通知订单服务改状态，实现**异步解耦 + 最终一致**。
5. 活动搜索走 **Elasticsearch** 索引检索，热门活动与票档详情走 **Redis 缓存** 降低数据库压力；网关与核心接口接入 **Sentinel** 限流降级。

---

## 四、技术亮点（每条含：实现细节 + 简历表述 + 面试可聊点）

### 亮点 1：网关统一鉴权 + JWT 非对称加密
**实现细节**
- 用户服务登录成功后签发 JWT（Hutool JWT，**RS256 非对称签名**，密钥对从 `hmall.jks` 密钥库经 `KeyStoreKeyFactory` 加载，Token TTL 30 分钟），密码使用 **BCrypt** 加密存储。
- 网关自定义 `AuthGlobalFilter`（GlobalFilter, order=0）：按白名单匹配放行 → 解析请求头 Token → `jwtTool.parseToken` 校验签名与过期 → 通过 `exchange.mutate()` 将 userId 写入 `user-info` 请求头 → 转发下游。
- 校验失败返回 401，`UnauthorizedException` 统一兜底。

**简历表述**
> 基于 Spring Cloud Gateway 实现 API 统一入口，自定义全局过滤器完成 JWT（RS256 非对称加密）登录鉴权与登录白名单放行；用户身份通过请求头透传、拦截器写入 ThreadLocal，实现一次登录全链路免鉴权。

**面试可聊点**：非对称签名的好处（网关只验签不存密钥）、Token 过期/吊销策略、为什么选 RSA 而非对称。

---

### 亮点 2：Seata 分布式事务保证跨服务下单一致性 + 库存防超卖
**实现细节**
- `OrderServiceImpl.createOrder()` 加 `@GlobalTransactional`，一个全局事务内串联 4 个跨服务写操作：
  1. 商品/明细落库（本地事务）
  2. 清理购物车（Feign 调 cart-service）
  3. 扣减库存（Feign 调 item-service，批量 executeBatch）
  4. 发送订单超时延迟消息
- 任一步失败全局回滚（例如扣库存异常 → 回滚订单与明细）。
- 扣库存 SQL 使用**条件更新防超卖**：`UPDATE item SET stock = stock - #{num} WHERE id = #{itemId} AND stock >= #{num}`，影响行数为 0 即视为库存不足并抛出业务异常，杜绝扣成负数。

**简历表述**
> 下单链路涉及订单、购物车、库存多服务写操作，采用 Seata AT 模式全局事务保证数据一致性，任一环节失败自动回滚；扣库存基于 `stock >= num` 条件更新实现原子扣减，并发下也不会超卖。

**面试可聊点**：AT 模式原理（分支事务 + 全局锁 + 回滚日志 undo_log）、乐观扣减与悲观锁对比、超卖如何被条件更新拦截、分布式事务与本地事务对比。

---

### 亮点 3：RabbitMQ 延迟消息实现订单超时自动关闭
**实现细节**
- 下单后通过 RabbitMQ **延迟消息插件（delayed = "true" 延迟交换机）**发送延迟 10s 的订单检查消息。
- `OrderDelayMessageListener` 消费时：先查订单状态，未支付则再调 pay-service 查支付流水，已支付 → 标记订单已支付；未支付 → **关闭订单并回补库存**（Feign 调 item-service 恢复库存）。
- `PayStatusListener` 监听 `pay.direct` 交换机 `pay.success` 路由键，收到支付成功事件后幂等更新订单状态为已支付。
- 统一通过 `Jackson2JsonMessageConverter` 做消息序列化（hm-common 的 MqConfig）。

**简历表述**
> 订单超时未支付场景使用 RabbitMQ 延迟消息实现延时检测，超时自动关单并恢复库存，替代定时轮询；支付成功通过 MQ 事件驱动订单状态更新，实现服务间异步解耦与最终一致性，消息体采用 Jackson JSON 序列化。

**面试可聊点**：延迟消息与 TTL+死信队列两种实现对比、消息可靠性与重复消费（消费幂等：先查状态再更新）、为什么不用 `@Scheduled` 轮询、关单时如何保证与已支付请求的并发一致性。

---

### 亮点 4：支付幂等设计 + 乐观锁防并发重复支付 + 支付结果事件通知
**实现细节**
- `applyPayOrder()` 基于**业务单号（bizOrderNo）做幂等**：已存在支付单直接返回，已支付/已关闭抛业务异常，支付渠道变更时重置支付单复用原 ID，保证一个订单最多一条支付单。
- 余额支付 `tryPayOrderByBalance`：先查支付单状态 → Feign 调 user-service 扣余额 → `markPayOrderSuccess` 用 **条件更新做乐观锁**（`in(status, NOT_COMMIT, WAIT_BUYER_PAY)`），状态不再待支付时更新失败并报"交易已支付或关闭"。
- 支付成功后通过 RabbitMQ 发送 `pay.success` 事件，trade-service 监听后更新订单状态，实现支付与订单的异步解耦。
- 支付单号采用 MyBatis-Plus `IdWorker`（雪花算法）生成。

**简历表述**
> 支付模块基于业务单号实现申请支付单幂等（一单一单、渠道变更复用重置），并通过带状态条件的乐观锁更新防止并发重复支付；支付成功后经 MQ 事件通知订单服务，异步解耦保障资金操作安全。

**面试可聊点**：乐观锁 vs 悲观锁、幂等键设计、雪花算法 ID 特性、支付结果通知丢失如何兜底（订单侧延迟消息主动查支付流水）。

---

### 亮点 5：OpenFeign 统一封装 + 用户上下文跨服务透传
**实现细节**
- hm-api 模块统一定义 Feign 客户端（Cart/Item/Pay/Trade/User），服务间调用不复用 HTTP 模板。
- `DefaultFeignConfig` 提供：FULL 级别日志、`RequestInterceptor` 自动将 ThreadLocal 中的 userId 回填到 `user-info` 请求头（解决 Feign 新线程丢上下文问题）、`ItemClientFallbackFactory` 降级兜底（配合 Sentinel 限流降级）。
- cart-service 查询购物车时 Feign 批量查询商品信息并内存聚合（Map 组装价格/库存/状态）。

**简历表述**
> 基于 OpenFeign 统一封装跨服务 HTTP 调用，通过 RequestInterceptor 自动透传用户上下文解决跨服务登录态丢失问题；为商品查询接口配置 FallbackFactory 降级兜底，提升链路容错。

**面试可聊点**：Feign 拦截器实现原理、上下文跨服务传递方案（Header + ThreadLocal）、FallbackFactory 与 Fallback 区别、为什么要用 loadbalancer 替代 Ribbon。

---

### 亮点 6：Elasticsearch 商品搜索
**实现细节**
- 为商品建立 ES 索引（`ItemDoc`），搜索接口通过 RestHighLevelClient 走 ES 检索，支持关键字/品牌/分类/价格区间多条件组合过滤、分页与相关性排序。
- 商品上下架、价格等变更同步维护索引，保证搜索数据与 MySQL 一致；搜索与详情读请求分流，降低数据库压力。

**简历表述**
> 基于 Elasticsearch 搭建商品搜索服务，建立商品索引实现关键字、品牌、分类、价格区间多条件组合检索与分页排序，搜索读请求从 MySQL 分流至 ES，显著提升检索性能。

**面试可聊点**：倒排索引原理、分词器选择、MySQL 与 ES 数据同步方案（双写/监听 binlog）、索引重建与更新策略。

---

### 亮点 7：Redis 热点缓存
**实现细节**
- 商品详情等热点数据缓存至 Redis，查询优先走缓存，缓存未命中回源 MySQL 并回填，降低热点商品对数据库的冲击。
- 商品变更时采用"先更新数据库、再删除缓存"策略，规避缓存与数据库不一致；合理设置过期时间兜底。

**简历表述**
> 引入 Redis 缓存商品热点数据，采用缓存优先读 + 更新数据库后失效缓存的策略，缓解高并发读对数据库的压力，热点数据命中率明显提升。

**面试可聊点**：缓存穿透/击穿/雪崩及对应方案（空值缓存、互斥锁、逻辑过期）、缓存一致性方案对比、过期策略。

---

### 亮点 8：Sentinel 限流降级
**实现细节**
- 网关与核心业务接口接入 Sentinel：为下单、商品查询等高 QPS 接口配置 QPS 限流规则，超阈值直接快速失败，保护下游服务。
- Feign 客户端配置 `FallbackFactory` 降级兜底，下游服务异常或熔断时返回兜底数据，避免故障级联。

**简历表述**
> 接入 Sentinel 为下单、查询等高流量接口配置 QPS 限流，并配合 OpenFeign 降级兜底，下游异常时返回兜底数据，防止故障在服务间级联扩散。

**面试可聊点**：限流算法（固定窗口/滑动窗口/漏桶/令牌桶）、熔断降级与隔离、Sentinel 与 Hystrix 对比。

---

### 亮点 9：公共组件沉淀 —— 统一异常/返回/上下文
**实现细节**
- hm-common 通过 `@ConditionalOnClass` 按需自动装配公共配置：MVC 拦截器、MyBatis-Plus 分页拦截器（含 maxLimit 保护）、RabbitMQ Jackson 转换器、JSON 序列化。
- `CommonExceptionAdvice`（@RestControllerAdvice）统一处理 DbException / CommonException / 参数校验异常等，返回统一 R 结构。
- `UserContext` 基于 ThreadLocal 存取当前登录用户，`UserInfoInterceptor` 拦截器负责写入与清理（afterCompletion 移除，防内存泄漏）。
- 分页统一封装 `PageQuery` / `PageDTO`，业务异常自定义异常体系（BadRequest / BizIllegal / Unauthorized / Forbidden）。

**简历表述**
> 抽取 hm-common 公共模块，沉淀统一异常处理、统一返回结构、分页封装与 ThreadLocal 用户上下文等基础设施，通过条件装配按需生效，减少各服务重复代码。

**面试可聊点**：ThreadLocal 泄漏风险与清理时机、@RestControllerAdvice 处理流程、条件装配 @ConditionalOnClass 的作用。

---

### 亮点 10：Nacos 配置中心 + 环境多 Profile 管理
**实现细节**
- 各服务通过 bootstrap.yaml 接入 Nacos Config，共享 `shared-jdbc` / `shared-log` / `shared-swagger` / `shared-seata` 公共配置，数据库连接、日志、文档、事务配置集中管理、热更新。
- 网关路由等配置由 Nacos 下发，服务端口、DB 地址按 `dev` / `local` 多环境 Profile 隔离。

**简历表述**
> 接入 Nacos 配置中心，公共配置（数据源/日志/接口文档/Seata）以共享配置方式下发，支持动态刷新与多环境（dev/local）隔离。

**面试可聊点**：配置中心热更新机制、bootstrap 与 application 加载顺序、共享配置 vs 服务独有配置。

---

### 亮点 11：MyBatis-Plus 深度使用
**实现细节**
- 条件构造器（lambdaQuery）实现动态条件查询（关键字/品牌/分类/价格区间/上下架状态）。
- 分页插件 + `PageQuery.toMpPage()` 统一分页、`PageDTO.of()` 统一封装。
- 批量操作（executeBatch 扣库存）、枚举状态处理（MybatisEnumTypeHandler）、条件更新/乐观锁。

**简历表述**
> 基于 MyBatis-Plus 实现动态条件分页查询、批量库存扣减与枚举状态映射，避免手写大量 SQL，提升开发效率。

---

## 五、简历"项目经验"可粘贴段落

> 写简历时建议从下面挑选 3~4 条加粗关键词句，勿整段堆砌。

**段落 A（偏架构，投 Java 后端/微服务岗）：**
> 参与热门赛事与演出门票交易平台微服务化开发，基于 Spring Cloud Alibaba 拆分为用户、活动票务、购票清单、订单、支付五大服务，接入 Nacos 注册与配置中心、Spring Cloud Gateway 网关。网关统一完成 JWT（RS256）登录鉴权与白名单放行，用户身份经请求头 + ThreadLocal + Feign 拦截器全链路透传。抢票下单流程使用 Seata 全局事务串联订单落库、清购票清单、锁定票源，票源扣减基于 `stock >= num` 条件更新防超卖；订单超时检测与支付成功通知基于 RabbitMQ 延迟消息与事件解耦实现，达到最终一致。支付模块通过业务单号幂等与乐观锁更新防止重复支付。活动搜索基于 Elasticsearch、热点数据基于 Redis 缓存、核心接口接入 Sentinel 限流降级。抽取 hm-common 公共模块沉淀统一异常、统一返回与分页组件，各服务依赖 Knife4j 独立维护接口文档。

**段落 B（偏业务实现，突出数据一致性/高可用思维）：**
> 负责票务订单与支付核心链路开发。① 基于 Seata AT 模式全局事务保障“创建订单—清理购票清单—锁定票源”跨服务一致性，余票不足时整体回滚，条件更新防止超卖；② 使用 RabbitMQ 延迟消息实现订单超时未支付自动关闭并回补票源，支付成功通过 MQ 事件驱动订单状态更新，替代定时轮询，提升系统吞吐与解耦性；③ 支付单按业务单号幂等创建，采用乐观锁条件更新防止并发重复支付，保障资金安全；④ 引入 ES 活动搜索、Redis 热点缓存、Sentinel 限流降级，降低数据库压力并提升链路可用性。

**段落 C（技能/关键词行）：**
> 微服务 · Spring Cloud Gateway · Nacos · OpenFeign · Seata · RabbitMQ · Elasticsearch · Redis · Sentinel · MySQL · MyBatis-Plus · JWT · Knife4j

---

## 六、面试高频追问与应答要点

| 可能被问 | 应答要点（结合本项目） |
|---|---|
| 网关为什么能拦截/怎么实现的 | GlobalFilter + Ordered，白名单 AntPathMatcher，校验失败 setStatusCode(401)；Netty 响应式，非 Servlet 容器 |
| JWT 为什么用 RS256 | 非对称，网关只持公钥验签即可，签发在用户服务，密钥可独立管理；项目用 JKS 密钥库托管密钥对 |
| ThreadLocal 泄漏怎么避免 | UserInfoInterceptor.afterCompletion 里 removeUser()，强调"用完即清" |
| Feign 丢失请求头怎么办 | 自定义 RequestInterceptor，从 UserContext 读取后回填 user-info 头 |
| Seata AT 怎么实现回滚 | 分支事务记录 undo_log，全局锁保证写冲突，两阶段提交；数据一致性由回滚日志恢复 |
| 怎么防超卖 | 扣库存 SQL 加 `stock >= num` 条件，影响行数为 0 即失败，配合 Seata 全局回滚 |
| 延迟消息为什么不用 @Scheduled | 延迟消息按需触发、集群下不重复扫描、无轮询压力；项目用 RabbitMQ delayed 插件 |
| 超时关单和支付并发怎么办 | 关单前主动查支付流水状态；订单状态更新用条件判断兜底，避免"已支付却被关单" |
| 消息重复消费怎么办 | 消费端幂等：先查订单状态，仅 status=1 才更新（PayStatusListener 里的状态判断） |
| 幂等怎么做的 | 业务单号查支付单，存在即复用；渠道变更才重置字段，保留原单号 |
| 为什么乐观锁 | 支付状态变更并发概率低，条件更新保证 CAS 语义，无需 DB 行锁 |
| ES 和 MySQL 数据怎么保持一致 | 商品变更双写/监听 binlog 同步 ES，变更后同时失效 Redis 缓存 |
| 缓存穿透/击穿/雪崩怎么防 | 空值缓存 / 互斥锁与逻辑过期 / 过期时间加随机抖动 + 多级缓存 |
| Sentinel 限流规则怎么配 | 下单、商品查询等高 QPS 接口按 QPS 阈值快速失败；Feign 降级兜底 |

---

## 七、可选的简历加分措辞

- "通过 MQ 异步化将支付结果通知与订单状态更新解耦，支付服务不需要同步等待订单服务，缩短响应时间。"
- "统一封装 R 返回体与全局异常处理器，前后端联调效率提升；接口全部由 Knife4j 自动生成在线文档。"
- "服务配置全部收敛到 Nacos 配置中心，多环境（dev/local）通过 Profile 一键切换，配置可动态刷新。"
- "购物车商品信息采用 Feign 批量查询 + 内存 Map 聚合，避免 N+1 次远程调用。"
- "搜索读请求走 ES、热点详情走 Redis，数据库承担写与少量读，读写分离降低主库压力。"
- "为高流量接口配置 Sentinel 限流 + Feign 降级，下游故障不级联，系统整体可用性提升。"

---

*文档生成时间：2026-08-16 · 基于 hmall 仓库 master 分支（commit 9ff808d）整理。*
