# 票务平台：简历展示与演示交付计划

## 目标

把已完成的票务 MVP 整理为一个可在面试中快速启动、演示并讲清技术亮点的项目。此计划不新增选座、真实支付、退款或运营后台。

## 交付标准

- 新同学按 README 可完成环境准备、建表和服务启动。
- 可在 5 分钟内演示“演出浏览 → 抢票 → 排队结果 → 模拟支付 → 出票 → 一次性验票”。
- 简历和面试陈述能对应到实际代码、接口和验证结果。

## 阶段 1：可启动性

### 工作项

1. 新增根目录 README，说明项目定位、模块职责、技术栈和明确不做项。
2. 提供基础依赖清单：MySQL、Redis、RabbitMQ、Nacos，以及建议版本和启动顺序。
3. 汇总 `event-schema.sql`、`order-schema.sql` 为一份初始化说明，并提供最小演示数据。
4. 为每个服务提供本地配置模板；敏感地址、账号和密码仅使用示例值或环境变量。
5. 提供一条全工程编译命令和单服务启动命令。

### 验收

- 在新环境中可按文档完成编译、建表和服务启动。
- README 不包含旧商城品牌或真实敏感凭据。

## 阶段 2：可演示性

### 演示脚本

1. 查询在售演出：`GET /shows`。
2. 查询场次：`GET /shows/{showId}/sessions`。
3. 查询票档：`GET /shows/sessions/{sessionId}/ticket-tiers`。
4. 开售前预热 Redis 余票：`PUT /ticket-rush/sessions/{sessionId}/tiers/{tierId}/stock`。
5. 发起抢票：`POST /ticket-rush`，得到排队请求号。
6. 查询结果：`GET /ticket-rush/{requestId}`，展示 `QUEUED` 到 `SUCCESS:{orderId}`。
7. 创建或模拟完成支付，展示支付成功 MQ 通知。
8. 查询电子票：`GET /ticket-orders/{orderId}/tickets`。
9. 核验电子票：`PUT /ticket-orders/tickets/{token}/verify`；重复调用应失败。
10. 额外演示未支付订单：等待或触发超时任务，验证订单取消和库存回补。

### 工作项

1. 编写 HTTP 请求集合（IntelliJ HTTP Client 或 Postman Collection）。
2. 准备一组固定演示数据：一场赛事、一个场次、三个票档。
3. 在接口文档中标注登录请求头、请求体和预期响应。
4. 提供演示时的故障处理说明：Nacos 未启动、MQ 未启动、Redis 未预热时如何判断和恢复。

### 验收

- 按 HTTP 请求集合可完整走通核心链路。
- 可复现售罄、超限购、重复验票和超时回补四类结果。

## 阶段 3：可讲述性

### 架构材料

1. 画一张服务架构图：网关、用户、演出、订单、支付、Redis、RabbitMQ、MySQL。
2. 画一张抢票时序图：限流 → Lua 预扣 → MQ → 异步建单 → 支付 → 出票。
3. 画一张订单状态图：`PENDING_PAYMENT → PAID → issued`，以及 `PENDING_PAYMENT → CANCELLED → stock restored`。
4. 为关键类添加简短注释，说明边界与幂等策略。

### 简历表述草案

> 基于 Spring Cloud Alibaba 构建赛事与演出票务平台，围绕热点场次实现 Redis Lua 原子预扣与场次限购，结合 RabbitMQ 异步建单和失败补偿避免超卖；完成 15 分钟超时关单回补、模拟支付成功事件、电子票生成及一次性验票闭环。

### 面试准备

- 为什么秒杀主链路不用 Seata：Redis 原子预扣 + MQ 最终一致性更适合热点写入。
- 如何防超卖：Lua 预扣、数据库条件扣减、消费失败回补三层控制。
- 如何处理重复消息：请求号消费幂等键与订单状态条件更新。
- 如何处理支付回调重复：仅允许待支付订单迁移为已支付，出票逻辑先查询已有票。
- 当前边界：模拟支付、票档售卖；选座和真实支付作为后续扩展。

## 推荐执行顺序

1. 阶段 1：README、配置模板、SQL 与演示数据。
2. 阶段 2：HTTP 请求集合与一次完整录屏/截图验证。
3. 阶段 3：架构图、时序图、简历描述与面试问答。

完成这三步后，项目即可进入投递和面试演示阶段。
