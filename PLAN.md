# 热门赛事与演出门票交易平台改造计划

## 项目定位与明确边界

这是一个用于简历展示的微服务票务项目，不以生产级商业平台为目标。重点是展示清晰的领域建模、服务协作、高并发防超卖、异步一致性和可观测的交易闭环。

项目只完成“按票档购票”的核心闭环：

`浏览演出 → 选择场次和票档 → 抢票/下单 → 15 分钟内模拟支付 → 出票 → 二维码模拟验票 → 未支付自动释放`

其中“选座”不属于本项目交付范围，实际流程为：

`浏览演出 → 选择场次和票档 → 抢票/下单 → 15 分钟内模拟支付 → 生成电子票 → 二维码模拟验票 → 超时释放库存`

实施采用渐进式路线：完成品牌清理、票档 MVP 与轻量级抢票链路即可结项。每个阶段完成验收后再进入下一阶段。

### 必做范围

- 演出、场次、票档、票档库存、观演人、订单、电子票的数据建模。
- 用户登录、按场次限购、开售时间校验、原子扣库存。
- Redis 预扣库存 + Lua 限购校验 + MQ 异步建单，展示高并发防超卖思路。
- 模拟支付成功、15 分钟延迟关单、库存回补、异步出票。
- 二维码/令牌生成与一次性模拟验票。
- 基础限流、幂等、日志和关键业务指标展示。

### 明确不做

- 不做真实第三方支付接入、真实退款、财务对账或资金安全体系；支付以内部模拟回调实现。
- 不做复杂选座、场馆座位图、锁座状态机；所有门票均按票档售卖。
- 不做真实身份证核验；观演人实名信息仅做格式校验与脱敏展示。
- 不做生产级风控、验证码厂商接入、设备指纹、反爬或黑产对抗。
- 不做主办方多租户后台、复杂营销、分销、结算和全量运营能力。
- 不追求跨地域容灾、百万级压测结果或生产级 SLA；仅提供本地/演示环境可复现的压测与监控数据。

### 简历可表述的实现边界

可强调“面向热点场次抢票的票档售卖场景，采用 Redis Lua 预扣 + MQ 异步建单防止超卖，并以延迟消息处理未支付订单的库存回补”；不要宣称已实现真实支付、生产级选座或大规模商业票务运营。

## 统一命名

| 范围 | 目标名称 |
| --- | --- |
| 仓库/根目录、Maven 根 `artifactId` | `ticket-platform` |
| Java 根包名 | `com.ticketing` |
| 公共模块 | `ticket-common` |
| 接口模块 | `ticket-api` |
| 网关 | `ticket-gateway` |
| 用户服务 | `user-service` |
| 演出服务 | `event-service` |
| 订单服务 | `order-service` |
| 支付服务 | `payment-service` |
| 购票清单（可选） | `ticket-service` |

其中 `event-service`、`order-service`、`payment-service` 分别替换商品、交易和支付的商城语义。根目录与远程仓库均保持 `ticket-platform`。

## 服务映射与职责

| 现有模块 | 改造后定位 | 主要职责 |
| --- | --- | --- |
| `user-service` | 用户与观演人 | 登录、实名观演人、实名校验状态 |
| `item-service` → `event-service` | 演出与票源 | 演出、场次、票档、场馆、座位、库存 |
| `cart-service` → `ticket-service` | 购票清单（可选） | 普通购票清单；秒杀链路直接下单 |
| `trade-service` → `order-service` | 票务订单与出票 | 锁票、订单、取消/退票、出票状态 |
| `pay-service` → `payment-service` | 支付与退款 | 幂等支付、超时、退款、支付成功事件 |
| `hm-gateway` → `ticket-gateway` | 抢票流量入口 | 登录校验、限流、验证码/排队令牌、黑名单 |
| `hm-api` → `ticket-api` | 跨服务契约 | 场次、锁票、出票、验票 DTO 与 Feign 接口 |
| `hm-common` → `ticket-common` | 公共能力 | 分布式锁、幂等、防刷、统一异常与基础组件 |

## 阶段 0：品牌与命名清理

### 工作项

1. 将 Maven `groupId`、`artifactId`、模块名、Spring 应用名统一为票务命名。
2. 将旧商城根包迁移为 `com.ticketing`，同步调整包路径、导入和测试。
3. 重命名 `hm-*` 服务、日志名、数据库名、MQ 虚拟主机、Nacos Data ID 和配置前缀。
4. 替换 Swagger 标题、联系人、网址、JWT 密钥文件名/别名、RabbitMQ 配置等安全与配置资产。
5. 清理可见配置、接口文档和示例响应中的旧商城品牌字样；历史简历素材可后续单独处理。

### 验收

- 排除构建产物和本计划后，旧商城品牌关键词搜索结果为 0。
- 所有模块重新编译通过。
- 登录、JWT 签发与验签完成回归验证。

## 阶段 1：票档抢票 MVP

先不实现复杂选座。每个场次配置多个票档（例如看台、内场、VIP），按可售数量完成售卖。

### 数据模型

| 表 | 用途 |
| --- | --- |
| `show` | 演出/赛事基本信息 |
| `show_session` | 场次、开售时间、演出时间、场馆 |
| `ticket_tier` | 票档、价格、总票数、可售票数、限购数 |
| `ticket_order` | 关联场次的票务订单 |
| `ticket_order_item` | 订单票档明细 |
| `audience` | 观演人实名信息 |
| `ticket` | 已出票电子票、二维码令牌、验票状态 |

### 核心规则与交付

1. 场次按开售和停售时间控制购买资格。
2. 用户按场次限购（MVP 默认支持配置，例如每人 2 张）。
3. 下单原子扣减票档库存，创建“待支付”订单。
4. 订单 15 分钟未支付自动取消并回补票源。
5. 支付成功事件异步生成电子票。
6. 电子票二维码一次性验票，重复核验必须拒绝。

### 优先复用

- 演出服务复用现有库存扣减模型作为票档库存的起点。
- 订单服务复用普通下单、延迟关单和事务编排能力。
- 支付服务复用幂等支付和 MQ 支付成功通知。
- 复用 Redis 热点缓存、ES 搜索、Sentinel 限流。

### 验收

- 完成从浏览场次、提交订单、支付、出票到验票的端到端测试。
- 并发下单不超卖；取消和超时订单均能正确回补库存。
- 同一用户超出场次限购时被拒绝；同一电子票无法重复入场。

## 阶段 2：轻量级高并发抢票链路

### 目标链路

`客户端 → 网关校验/限流/排队 → Redis 校验与预扣 → MQ → 订单服务落库确认锁票 → 支付 → 支付成功出票`

### 工作项

1. 开售前将每个票档库存预热至 `ticket:stock:{sessionId}:{tierId}`。
2. 以 Lua 脚本原子完成开售校验、用户限购、余票校验、Redis 预扣及用户抢票结果记录。
3. Redis 预扣成功后投递 MQ，由消费者异步创建订单，接口返回排队中/抢票成功/已售罄。
4. 提供订单结果轮询或 SSE/WebSocket 通知。
5. 为消息定义业务幂等键、重试和死信处理；消费失败必须补偿 Redis 库存。
6. 网关按场次与用户/IP 限流，加入验证码、排队令牌、黑名单和热点防穿透措施。
7. 将 Seata 保留给普通下单一致性；秒杀主链路以 Redis 原子预扣、MQ 最终一致性与补偿机制为准。

### 验收

- 演示压测下库存不为负、订单不重复、消息可追溯。
- 消费失败、重复消息、支付回调重复与延迟消息乱序均能被正确处理。
- 库存差异、消息堆积、限流拒绝和补偿次数具备监控指标。

## 后续扩展（不纳入本项目交付）：选座

### 新增模型

`venue`、`venue_area`、`seat`、`session_seat`、`seat_lock`。

### 座位状态机

`AVAILABLE → LOCKED → SOLD`

支付超时或取消时：`LOCKED → AVAILABLE`。

### 工作项与约束

1. 提供场次座位图与可选状态；不向前端暴露不必要的敏感库存数据。
2. 用 `WHERE status = 'AVAILABLE'` 的条件更新锁定单座位。
3. 多座位采用批量条件更新并检查影响行数：全部锁定才成功，否则全部失败。
4. Redis 可缓存座位图，但数据库锁座记录是最终事实来源。
5. 锁座有效期配置为 5–10 分钟；超时任务释放座位并回补票源。

### 验收

- 并发下同一座位只能被一个订单锁定。
- 多座位请求不存在部分成功的脏状态。
- 取消、支付超时和支付成功都能驱动正确状态迁移。

## 后续扩展（不纳入本项目交付）：真实票务运营

1. 实名观演人、一票一证与实名校验。
2. 带签名的电子票二维码，支持动态刷新或一次性令牌。
3. 检票端：验签、验票、幂等写入入场时间。
4. 基于演出时间和票种规则的退票判定，及支付渠道退款回调。
5. 主办方后台：演出、场次、票档、座位图、售票时间管理。
6. 风控：频繁失败限制、设备/IP 限流、黑名单、验证码。
7. 可观测性：订单状态、库存差异、MQ 堆积、锁票超时、支付回调失败监控与告警。

## 推荐执行顺序（本项目）

1. 执行阶段 0，建立独立的票务品牌、命名和配置基线。
2. 将商品域拆分为演出、场次、票档和票源，完成阶段 1 的数据与接口设计。
3. 改造订单域以支持观演人、锁票、出票、取消和退票状态。
4. 接入延迟消息，完成支付超时释放票源。
5. 基于 Redis + MQ 实现阶段 2 的预扣和异步建单。
6. 补齐模拟验票、基础监控、接口文档和演示数据，整理为可部署、可演示、可写入简历的版本。

阶段 3 和阶段 4 仅作为后续可扩展方向，不排入当前开发计划。

## 当前可执行演示

`ticket-mvp-demo` 是一个不依赖 Nacos、MySQL、Redis 或 MQ 的独立 Maven 可执行模块，用于演示本计划的核心交易状态和验收用例。执行方式：

```powershell
mvn -f ticket-mvp-demo/pom.xml package
java -jar ticket-mvp-demo/target/ticket-mvp-demo-1.0.0.jar
```

它使用内存原子操作模拟库存预扣；后续接入现有微服务时，将该原子段替换为 Redis Lua，并将建单与出票拆为 MQ 消费者即可。

## 里程碑

| 里程碑 | 完成条件 |
| --- | --- |
| M0：去品牌化 | 命名清理验收通过，所有服务可启动 |
| M1：可交易 | 票档购票、支付、出票、验票、超时释放闭环可用 |
| M2：可抢票 | Redis 预扣、MQ 异步建单、限流限购和补偿可用 |
| M3：可演示 | 模拟验票、基础监控、接口文档、演示数据和部署说明齐备 |
