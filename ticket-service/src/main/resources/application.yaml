server:
  port: 8082
  tomcat:
    threads:
      max: 25
    accept-count: 25
    max-connections: 100
feign:
  okhttp:
    enabled: true
#  sentinel:
#    enabled: true
ticket:
  db:
    database: ticket_list
  swagger:
    title: "票务平台购物车服务接口文档"
    package: com.ticketing.cart.controller
spring:
  cloud:
    sentinel:
      transport:
        dashboard: localhost:8090 # sentinel的控制台地址
      http-method-specify: true # 是否设置请求方式作为资源名称